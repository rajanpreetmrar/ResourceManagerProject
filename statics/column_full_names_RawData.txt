CREATE TABLE RawData (
    `Report Record Number` INT,
    `Provider CCN` INT,
    `Hospital Name` VARCHAR(255),
    `Street Address` VARCHAR(255),
    `City` VARCHAR(255),
    `State Code` VARCHAR(2),
    `Zip Code` VARCHAR(10),
    `County` VARCHAR(255),
    `Medicare CBSA Number` INT,
    `Rural Versus Urban` CHAR(1),
    `CCN Facility Type` INT,
    `Provider Type` INT,
    `Type of Control` INT,
    `Fiscal Year Begin Date` DATE,
    `Fiscal Year End Date` DATE,
    `FTE - Employees on Payroll` FLOAT,
    `Number of Interns and Residents (FTE)` FLOAT,
    `Total Days Title V` INT,
    `Total Days Title XVIII` INT,
    `Total Days Title XIX` INT,
    `Total Days (V + XVIII + XIX + Unknown)` INT,
    `Number of Beds` INT,
    `Total Bed Days Available` INT,
    `Total Discharges Title V` INT,
    `Total Discharges Title XVIII` INT,
    `Total Discharges Title XIX` INT,
    `Total Discharges (V + XVIII + XIX + Unknown)` INT,
    `Total Days Title V + Total for all Subproviders` INT,
    `Total Days Title XVIII + Total for all Subproviders` INT,
    `Total Days Title XIX + Total for all Subproviders` INT,
    `Total Days (V + XVIII + XIX + Unknown) + Total for all Subproviders` INT,
    `Number of Beds + Total for all Subproviders` INT,
    `Total Bed Days Available + Total for all Subproviders` INT,
    `Total Discharges Title V + Total for all Subproviders` INT,
    `Total Discharges Title XVIII + Total for all Subproviders` INT,
    `Total Discharges Title XIX + Total for all Subproviders` INT,
    `Total Discharges (V + XVIII + XIX + Unknown) + Total for all Subproviders` INT,
    `Hospital Total Days Title V For Adults & Peds` INT,
    `Hospital Total Days Title XVIII For Adults & Peds` INT,
    `Hospital Total Days Title XIX For Adults & Peds` INT,
    `Hospital Total Days (V + XVIII + XIX + Unknown) For Adults & Peds` INT,
    `Hospital Number of Beds For Adults & Peds` INT,
    `Hospital Total Bed Days Available For Adults & Peds` INT,
    `Hospital Total Discharges Title V For Adults & Peds` INT,
    `Hospital Total Discharges Title XVIII For Adults & Peds` INT,
    `Hospital Total Discharges Title XIX For Adults & Peds` INT,
    `Hospital Total Discharges (V + XVIII + XIX + Unknown) For Adults & Peds` INT,
    `Cost of Charity Care` FLOAT,
    `Total Bad Debt Expense` FLOAT,
    `Cost of Uncompensated Care` FLOAT,
    `Total Unreimbursed and Uncompensated Care` FLOAT,
    `Total Salaries From Worksheet A` FLOAT,
    `Overhead Non-Salary Costs` FLOAT,
    `Depreciation Cost` FLOAT,
    `Total Costs` FLOAT,
    `Inpatient Total Charges` FLOAT,
    `Outpatient Total Charges` FLOAT,
    `Combined Outpatient + Inpatient Total Charges` FLOAT,
    `Wage-Related Costs (Core)` FLOAT,
    `Wage-Related Costs (RHC/FQHC)` FLOAT,
    `Total Salaries (adjusted)` FLOAT,
    `Contract Labor` FLOAT,
    `Wage Related Costs for Part - A Teaching Physicians` FLOAT,
    `Wage Related Costs for Interns and Residents` FLOAT,
    `Cash on Hand and in Banks` FLOAT,
    `Temporary Investments` FLOAT,
    `Notes Receivable` FLOAT,
    `Accounts Receivable` FLOAT,
    `Less: Allowances for Uncollectible Notes and Accounts Receivable` FLOAT,
    `Inventory` FLOAT,
    `Prepaid Expenses` FLOAT,
    `Other Current Assets` FLOAT,
    `Total Current Assets` FLOAT,
    `Land` FLOAT,
    `Land Improvements` FLOAT,
    `Buildings` FLOAT,
    `Leasehold Improvements` FLOAT,
    `Fixed Equipment` FLOAT,
    `Major Movable Equipment` FLOAT,
    `Minor Equipment Depreciable` FLOAT,
    `Health Information Technology Designated Assets` FLOAT,
    `Total Fixed Assets` FLOAT,
    `Investments` FLOAT,
    `Other Assets` FLOAT,
    `Total Other Assets` FLOAT,
    `Total Assets` FLOAT,
    `Accounts Payable` FLOAT,
    `Salaries, Wages, and Fees Payable` FLOAT,
    `Payroll Taxes Payable` FLOAT,
    `Notes and Loans Payable (Short Term)` FLOAT,
    `Deferred Income` FLOAT,
    `Other Current Liabilities` FLOAT,
    `Total Current Liabilities` FLOAT,
    `Mortgage Payable` FLOAT,
    `Notes Payable` FLOAT,
    `Unsecured Loans` FLOAT,
    `Other Long Term Liabilities` FLOAT,
    `Total Long Term Liabilities` FLOAT,
    `Total Liabilities` FLOAT,
    `General Fund Balance` FLOAT,
    `Total Fund Balances` FLOAT,
    `Total Liabilities and Fund Balances` FLOAT,
    `DRG Amounts Other Than Outlier Payments` FLOAT,
    `DRG amounts before October 1` FLOAT,
    `DRG amounts after October 1` FLOAT,
    `Outlier payments for discharges` FLOAT,
    `Disproportionate Share Adjustment` FLOAT,
    `Allowable DSH Percentage` FLOAT,
    `Managed Care Simulated Payments` FLOAT,
    `Total IME Payment` FLOAT,
    `Inpatient Revenue` FLOAT,
    `Outpatient Revenue` FLOAT,
    `Gross Revenue` FLOAT,
    `Less Contractual Allowance and discounts on patients' accounts` FLOAT,
    `Net Patient Revenue` FLOAT,
    `Less Total Operating Expense` FLOAT,
    `Net Income from Service to Patients` FLOAT,
    `Total Other Income` FLOAT,
    `Total Income` FLOAT,
    `Total Other Expenses` FLOAT,
    `Net Income` FLOAT,
    `Cost To Charge Ratio` FLOAT,
    `Net Revenue from Medicaid` FLOAT,
    `Medicaid Charges` FLOAT,
    `Net Revenue from Stand-Alone SCHIP` FLOAT,
    `Stand-Alone SCHIP Charges` FLOAT
);
